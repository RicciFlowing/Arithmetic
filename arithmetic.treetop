grammar Arithmetic

rule additive
  multitive ('+' multitive)*
end

rule multitive
  term ('*' term)*
end

rule term
  number / boolean / variable
end

rule number
  [0-9]+
end

rule boolean
  ('true' / 'false') ![a-z]
end

rule variable
  [a-z]+
end

end
